[project]
name = "fastapi-service"
version = "0.1.0"
description = "Placeholder FastAPI service for mzansi-spares-platform"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
